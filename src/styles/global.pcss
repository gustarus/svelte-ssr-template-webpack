#app {
  min-width: 100vw;
  min-height: 100vh;

  &.blurred {
    filter: blur(3px);
  }
}

#processing {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background: rgba(0, 0, 0, 0.2);
}
